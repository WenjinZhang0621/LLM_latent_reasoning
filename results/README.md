# Results Directory

Pipeline outputs are written here.

Expected artifact types:

- `*.jsonl` for generated CoTs, token features, steering outputs, and logs.
- `*.npz` for sparse or dense feature matrices.
- `*.pt` for steering vectors.
- `*.json` / `*.csv` / `*.md` for summaries.

Large generated artifacts should not be committed.
